# Copie para `frontend/.env.local` em desenvolvimento local.
# Nao commite `frontend/.env.local`.
#
# Variaveis com NEXT_PUBLIC_ ficam expostas no navegador.
# Nao coloque segredos neste arquivo.

NEXT_PUBLIC_APP_NAME=Fadex Helpdesk
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
